//Utilize um loop while para exibir apenas os números pares entre 1 e 20. 
let numero: number = 1;

while (numero <= 20) {
    if (numero % 2 === 0) {
        console.log(numero);
    }

    numero++;
}