//Utilize um loop for combinado com uma condicional para exibir apenas os números ímpares entre 1 e 15. 

for (let numero = 1; numero <= 150; numero++) {
    if (numero % 2 !== 0) {
        console.log(numero);
    }
}